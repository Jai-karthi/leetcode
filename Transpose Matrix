class Solution:
    def transpose(self, matrix: List[List[int]]) -> List[List[int]]:
        ans = []
        for i in range(len(matrix[0])):
            takeit = []
            for m in matrix:
                takeit.append(m[i])
            ans.append(takeit)
        return ans
