class Solution:
    def countSeniors(self, details: List[str]) -> int:
        count = 0
        for passangers in details :
            if int(passangers[-4:-2]) > 60 :
                count += 1
        return count
