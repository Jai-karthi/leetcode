class Solution:
    def sortPeople(self, names: List[str], heights: List[int]) -> List[str]:
        s = [(i,j) for i,j in zip(heights,names)]
        ans = [j for i ,j in sorted(s)[::-1]]
        return ans
