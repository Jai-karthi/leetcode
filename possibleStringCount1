from collections import Counter
class Solution:
    def possibleStringCount(self, word: str) -> int:
        ans= 0 
        l = len(word)
        for i in range(1,l):
            if word[i] == word[i-1] :
                ans +=1 

        return ans + 1
